import React from 'react';
import s from './Settings.module.css';

let Settings = () => {
  return (
    <div className={`grid__element content ${s.settings}`}>
      Settings
    </div>
  );
}

export default Settings;
